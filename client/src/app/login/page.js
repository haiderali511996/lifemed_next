import LoginClient from './LoginClient';

export const metadata = {
  title: 'Login — Lifemed Pharma',
  description: 'Log in to your Lifemed Pharma account to track orders and checkout faster.',
};

export default function LoginPage() {
  return <LoginClient />;
}
